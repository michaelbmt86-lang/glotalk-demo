name: glotalk_v3_cross
description: "GloTalk V3 — 设备端离线 STT + 翻译 + TTS 跨语言实时通话"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets 等模型文件下载到手机后再开启
  # assets:
  #   - assets/models/
